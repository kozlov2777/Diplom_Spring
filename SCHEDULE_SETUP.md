# 📅 Система управління графіком роботи - Інструкція

## 🚀 Перший запуск

### 1. Після запуску додатку виконайте SQL скрипт:

Виконайте файл `src/main/resources/init_shifts.sql` в вашій базі даних MariaDB:

```sql
-- Ініціалізація таблиці змін
INSERT INTO shifts (id, name, start_time, end_time) VALUES
(1, 'Перша половина дня', '08:00:00', '16:00:00'),
(2, 'Друга половина дня', '16:00:00', '00:00:00');

-- Налаштування за замовчуванням
INSERT INTO schedule_settings (shift_id, waiters_count, cooks_count) VALUES
(1, 2, 1),
(2, 2, 1);
```

### 2. Переконайтесь що у вас є працівники:

У системі мають бути зареєстровані:
- Офіціанти (role_id = 3)
- Кухарі (role_id = 4)

---

## 📋 Функціональність

### 1. **Перегляд графіку**
- Перейдіть в `/schedule`
- Побачите графік на поточний тиждень
- Навігація: попередній/наступний тиждень

### 2. **Автогенерація графіку**
- Виберіть період (від - до)
- Натисніть "Згенерувати"
- Система автоматично розподілить зміни

**Алгоритм:**
- Рівномірний розподіл змін між працівниками
- 2 офіціанти + 1 кухар на кожну зміну (налаштовується)
- Автоматичні вихідні кожні 5 днів

### 3. **Налаштування**
- Перейдіть в "Налаштування"
- Вкажіть кількість офіціантів та кухарів для кожної зміни
- Зберігається в БД

### 4. **Редагування графіку**
- Натисніть "Редагувати" біля працівника
- Змініть зміну або позначте вихідний
- Зберігається одразу

### 5. **Статистика**
- Перегляньте скільки днів відпрацював кожен
- Виберіть період
- Побачите робочі дні/вихідні/години

### 6. **Експорт в Excel**
- Натисніть "📥 Експорт в Excel"
- Завантажиться файл .xlsx
- Відкрийте в Excel/LibreOffice

### 7. **Запити на зміну графіку**

**Для працівника:**
1. Натисніть "Запит" біля своєї зміни
2. Вкажіть причину (хвороба, особисті обставини)
3. Надішліть запит

**Для адміна:**
1. Перейдіть в "📋 Запити на зміну"
2. Побачите всі активні запити
3. Схваліть (✓) або Відхиліть (✗)
4. При схваленні - день автоматично стає вихідним

---

## 🗂️ Структура БД

### Таблиці:
- `shifts` - типи змін
- `work_schedules` - графік працівників
- `schedule_settings` - налаштування кількості працівників
- `shift_change_requests` - запити на зміну

---

## 🎯 Endpoints

| URL | Опис |
|-----|------|
| `/schedule` | Перегляд графіку |
| `/schedule/settings` | Налаштування |
| `/schedule/stats` | Статистика |
| `/schedule/generate` | Автогенерація |
| `/schedule/export/excel` | Експорт в Excel |
| `/shift-change-requests` | Запити на зміну |

---

## ✅ Переваги системи

✔ **Автоматизація** - генерація за 1 клік
✔ **Гнучкість** - редагування вручну
✔ **Справедливість** - рівномірний розподіл
✔ **Статистика** - контроль робочого часу
✔ **Експорт** - роздрук графіків
✔ **Комунікація** - запити на зміну

---

## 🔧 Якщо щось не працює:

1. Перевірте чи виконали SQL скрипт ініціалізації
2. Переконайтесь що є працівники з role_id = 3 або 4
3. Перевірте консоль на помилки
4. Переконайтесь що MariaDB запущена

---

Успіхів! 🚀

